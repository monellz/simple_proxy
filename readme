加了一个简易的加密解密，文件是encrypt.py，里面的函数encrypt既能加密又能解密。

比如说密钥是b'thisisakey'，明文是要传输的正文，比如说b'GET XXXXXX...'，他就把密钥重复数次和明文长度相同，然后把密钥和明文做异或得到密文，然后进行传输。这里加密解密的函数是一样的。

这个函数两个参数，第一个参数是明文/密文，第二个参数是密钥，返回值是密文/明文。

效率很低，卡的一批。



encrypt放到了util里面，换了可能更快的encrypt实现

添加了username跟password